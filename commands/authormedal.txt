$(eval t=$(urlfetch json https://trackmania.io/api/officialcampaign/CAMPAIGN_ID).playlist,m=Number.parseInt(decodeURIComponent("$(querystring)")); if(Number.isInteger(m)&m>0&m<=t.length){map=t[m-1];s=map.authorScore;l=s>60000?15:17; "The Author Time for "+map.name+" is "+(new Date(s).toISOString().slice(l, -1))}else{"Please provide a proper track number"})
